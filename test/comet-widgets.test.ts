import * as CometWidgets from '../src/index'

// TODO tests
describe('Dummy test', () => {
  it('works if true is truthy', () => {
    expect(true).toBeTruthy()
  })

  // it("CometAlbum is instantiable", () => {
  //   expect(new CometWidgets.carousel()).toBeInstanceOf(CometWidgets.carousel)
  // })
})
